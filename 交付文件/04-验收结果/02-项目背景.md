## 项目背景

### 大模型的"幻觉"问题
大模型在面对不知道的问题时，会编造听起来合理的答案——这就是"幻觉"。

### RAG 如何解决？
RAG（检索增强生成）不靠模型记忆，而是配一个"资料库"：
1. 用户提问 → 先到知识库检索相关文档
2. 检索结果 + 用户问题 → 拼接成 Prompt
3. 大模型基于检索到的文档生成回答

**核心价值**：大模型负责"说人话"，知识库负责"说实话"
