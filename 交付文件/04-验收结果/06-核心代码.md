## 核心代码

### Prompt 模板
```python
SYSTEM_PROMPT = """你是楚雄师范学院校园智能助手...
规则：
1. 只能基于【参考文档】中的内容回答问题
2. 文档中没有的信息，明确说未找到
3. 禁止编造数据"""

PROMPT_TEMPLATE = """【参考文档】
{context}

【用户问题】
{question}

请基于以上参考文档回答用户的问题。"""
```

### 检索逻辑
```python
def search(collection, query, n_results=3):
    # 1. 将问题转为向量
    query_vector = get_embeddings([query])[0]
    # 2. 在 ChromaDB 中搜索最相似的文档
    results = collection.query(
        query_embeddings=[query_vector],
        n_results=n_results,
    )
    # 3. 返回文档内容 + 来源 + 相似度距离
    return [{"content":..., "source":..., "distance":...}]
```

### 文件结构
```
workspace/
├── api_client.py          # DeepSeek + Qwen API 封装
├── document_loader.py     # 文档加载与切片
├── vector_store.py        # ChromaDB 向量存储与检索
├── rag_agent.py           # RAG Agent 组装
├── app.py                 # Gradio Web 界面
├── build_index.py         # 知识库索引构建
├── docs/                  # 10 篇知识文档
└── chroma_db/             # 向量数据库
```
